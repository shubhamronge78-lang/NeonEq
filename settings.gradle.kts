pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradle()
    }
}
dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "NeonEQ"
include(":app")
